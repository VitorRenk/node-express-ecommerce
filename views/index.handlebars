<div class="mb-6">
  <h1 class="text-3xl font-bold text-indigo-600 mb-2">Bem-vindo à Loja! 🛍️</h1>
  <p class="text-gray-600">Confira nossos produtos incríveis</p>
</div>

<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
  {{#each produtos}}
    <div
      class="bg-white rounded-lg shadow-md p-4 hover:shadow-lg transition flex flex-col h-full"
    >
      <div class="text-5xl mb-4 text-center">{{this.imagem}}</div>

      <h2 class="text-xl font-bold text-gray-800 mb-2">{{this.nome}}</h2>
      <p class="text-gray-600 text-sm mb-4 flex-grow">{{this.descricao}}</p>

      <div class="flex justify-between items-center gap-3 mt-auto">
        <span class="text-2xl font-bold text-indigo-600">R$
          {{this.preco}}</span>

        <form
          method="POST"
          action="/carrinho/adicionar"
          style="display: inline;"
        >
          <input type="hidden" name="produtoId" value="{{this.id}}" />
          <button
            class="bg-indigo-600 text-white px-8 py-2 rounded-lg hover:bg-indigo-800 transition whitespace-nowrap"
          >
            🛒 Add
          </button>
        </form>
      </div>
    </div>
  {{/each}}
</div>