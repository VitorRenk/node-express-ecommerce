<div class="max-w-4xl mx-auto">
  <h1 class="text-3xl font-bold text-indigo-600 mb-6">Meu Carrinho</h1>

  {{#if carrinho}}
    <div class="bg-white rounded-lg shadow-md p-6 mb-6">
      <table class="w-full">
        <thead class="border-b-2 border-gray-300">
          <tr>
            <th class="text-left py-2">Produto</th>
            <th class="text-right py-2">Preço</th>
          </tr>
        </thead>
        <tbody>
          {{#each carrinho}}
            <tr class="border-b border-gray-200 hover:bg-gray-50">
              <td class="py-4">{{this.nome}}</td>
              <td class="text-right py-4">R$ {{this.preco}}</td>
            </tr>
          {{/each}}
        </tbody>
      </table>

      <div class="mt-6 text-right border-t-2 pt-4">
        <h2 class="text-2xl font-bold text-indigo-600">
          Total: R$
          {{total}}
        </h2>
      </div>

      <div class="mt-6 flex gap-4 justify-end">
        <a
          href="/"
          class="bg-gray-600 text-white px-6 py-3 rounded-lg hover:bg-gray-800 transition"
        >
          ← Continuar comprando
        </a>

        <form method="POST" action="/carrinho/limpar" style="display: inline;">
          <button
            class="bg-red-600 text-white px-6 py-3 rounded-lg hover:bg-red-800 transition"
          >
            🗑️ Limpar carrinho
          </button>
        </form>

        <button
          class="bg-green-600 text-white px-6 py-3 rounded-lg hover:bg-green-800 transition"
        >
          ✅ Finalizar compra
        </button>
      </div>
    </div>
  {{else}}
    <div class="bg-white rounded-lg shadow-md p-8 text-center">
      <p class="text-xl text-gray-600 mb-4">Seu carrinho está vazio 😢</p>
      <a
        href="/"
        class="inline-block bg-indigo-600 text-white px-6 py-3 rounded-lg hover:bg-indigo-800 transition"
      >
        Voltar às compras 🛍️
      </a>
    </div>
  {{/if}}
</div>