<html lang="pt-BR">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{titlePage}}</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body
    class="bg-gray-100 text-gray-800 antialiased min-h-screen flex flex-col"
  >

    <!-- NAVBAR -->
    <nav class="bg-white shadow-md p-4 sticky top-0 z-10" role="navigation">
      <div
        class="container mx-auto flex justify-between items-center max-w-7xl"
      >
        <a
          href="/"
          class="text-2xl font-bold text-indigo-600 hover:text-indigo-800 transition duration-300"
        >
          Home 🏠
        </a>

        <div class="flex items-center gap-6">
          <div class="text-lg font-medium text-gray-700">
            Olá,
            {{user}}
            👋
          </div>

          <a
            href="/carrinho"
            class="relative bg-indigo-600 text-white px-4 py-2 rounded-lg hover:bg-indigo-800 transition"
          >
            🛒 Carrinho
            {{#if totalCarrinho}}
              <span
                class="absolute -top-2 -right-2 bg-red-600 text-white rounded-full w-6 h-6 flex items-center justify-center text-xs font-bold"
              >
                {{totalCarrinho}}
              </span>
            {{/if}}
          </a>
        </div>
      </div>
    </nav>

    <!-- MAIN CONTENT -->
    <main id="main-content" class="flex-grow">
      <div class="container mx-auto max-w-7xl p-4">
        {{{body}}}
      </div>
    </main>

    <!-- FOOTER -->
    <footer class="bg-gray-800 text-white p-4 mt-8" role="contentinfo">
      <div class="container mx-auto text-center text-sm max-w-7xl">
        <p>&copy; {{currentYear}} E-commerce. Feito por Vitor.</p>
        <p class="text-gray-400 mt-1">Desenvolvido com Express, Handlebars e
          Tailwind CSS.</p>
      </div>
    </footer>

  </body>
</html>